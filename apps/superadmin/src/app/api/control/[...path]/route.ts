import { cookies } from "next/headers";
import { NextRequest,NextResponse } from "next/server";
const API=process.env.SUPERADMIN_API_URL??"http://127.0.0.1:4001";
async function proxy(req:NextRequest,{params}:{params:Promise<{path:string[]}>}){const {path}=await params;if(path.join("/")==="auth/logout"){const response=NextResponse.json({ok:true});response.cookies.delete("sa_session");return response}const target=`${API}/superadmin/${path.join("/")}`;const body=req.method==="GET"||req.method==="DELETE"?undefined:await req.text();const token=(await cookies()).get("sa_session")?.value;const upstream=await fetch(target,{method:req.method,headers:{"content-type":"application/json",...(token?{authorization:`Bearer ${token}`}:{})},body,cache:"no-store"});const text=await upstream.text();const response=new NextResponse(text,{status:upstream.status,headers:{"content-type":upstream.headers.get("content-type")??"application/json","cache-control":"no-store"}});if(path.join("/")==="auth/login"&&upstream.ok){const data=JSON.parse(text);response.cookies.set("sa_session",data.sessionToken,{httpOnly:true,secure:true,sameSite:"strict",path:"/",maxAge:1800});}return response}
export const GET=proxy;export const POST=proxy;export const PUT=proxy;export const PATCH=proxy;export const DELETE=proxy;
